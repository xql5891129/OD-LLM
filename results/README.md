# Final OD-LLM Results

This directory contains the compact test-set metrics for the final OD-LLM
model. Raw datasets, checkpoints, training histories, baseline outputs, and
paper plotting artifacts are excluded from the public repository.

The line-bus results cover three regions at 30- and 60-minute resolutions.
`metroflow_top80` contains the public MetroFlow transfer experiment.
