inherits: real_od_qwen.yaml

experiment:
  name: od_llm_qwen_metroflow_full

data:
  path: data/metroflow_full/od.npy
  input_len: 12
  pred_len: 3

train:
  batch_size: 1

model:
  rank: 8
  num_source_tokens: 1000

