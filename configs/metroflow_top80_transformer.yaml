inherits: real_od_transformer.yaml

experiment:
  name: od_tensor_transformer_metroflow_top80

data:
  path: data/metroflow_top80/od.npy
  input_len: 12
  pred_len: 3

train:
  amp: true
  amp_dtype: bf16
  compute_train_metrics: false
